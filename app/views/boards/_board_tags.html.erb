<ul class="tags">
  <li><%= link_to("All Topics", board_path(@board), :class => "label label-default") %></li>
  <% @tags.each do |tag| %>
      <li class="tags"><%= link_to( tag.name, board_path(@board, tag: tag), :class => "label label-default tag") %></li>
  <% end %>
</ul>