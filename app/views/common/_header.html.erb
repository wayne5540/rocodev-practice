<div class="navbar navbar-default">
  <div class="container">
    <div class="navbar-header">
      <%= link_to("Rocodev Disq", root_path, :class => "navbar-brand") %>
    </div>
    <ul class="nav navbar-nav navbar-right">
      <li><%= link_to("所有看版", boards_path) %></li>

      <% if current_user.blank? %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">Join Us<b class="caret"></b></a>
          <ul class="dropdown-menu">
            <li><%= link_to("登入",new_user_session_path) %></li>
            <li><%= link_to("註冊",new_user_registration_path) %></li>
          </ul>
        </li>
      <% else %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= current_user.name %><b class="caret"></b></a>
          <ul class="dropdown-menu">
            <li><%= link_to("Topics管理", account_topics_path) %></li>
            <% if current_user.is_admin %>
              <li class="divider"></li>
              <li><%= link_to("New Board", new_admin_board_path) %></li>
              <li><%= link_to("看版管理(admin)", admin_boards_path) %></li>
            <% end %>
            <li class="divider"></li>
            <li><%= link_to("Logout", destroy_user_session_path, :method => :delete) %></li>
          </ul>
        </li>
      <% end %>
    </ul>
  </div>
</div>