<%= simple_form_for([:admin, @board], html: {class: 'form-inline'}) do |f|  %>
  <%= f.input :name, :required => true %>
  <%= f.input :description, :required => true %>
  <%= f.submit "Submit", :disable_with => "Submiting..." %>
<% end %>