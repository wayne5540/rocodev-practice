<%= simple_form_for([:admin, @board], html: { class: 'form-horizontal' }) do |f|  %>
  <%= f.input :name, :required => true %>
  <%= f.input :description, :required => true %>
  <%= f.button :submit, "Submit", :disable_with => "Submiting..." %>
<% end %>

